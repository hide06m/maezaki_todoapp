# 要件定義書

## 1. 目的
このアプリは、ToDo を登録できるようにする。
このアプリは、ToDo を確認できるようにする。
このアプリは、ToDo を編集できるようにする。
このアプリは、ToDo を削除できるようにする。
ToDo は「やること」の意味である。

## 2. システムで扱うデータ
データベースには `todos` テーブルを 1 つ使う。
テーブルは、ToDo 1 件ごとの情報を保存するための表である。

### 2-1. 保存する項目
| 項目名 | 意味 | 型 | 補足 |
|---|---|---|---|
| `id` | 1件を区別する番号 | `BIGINT` | 自動で入る主キー（データを1件ずつ見分けるための番号） |
| `title` | やること | `VARCHAR(255)` | 必須 |
| `detail` | メモ | `VARCHAR(255)` | 任意 |
| `category` | ジャンル | `VARCHAR(255)` | 必須 |
| `priority` | 優先度 | `INT` | 必須。1, 2, 3 のいずれか |
| `due_date` | 期限 | `DATE` | 任意 |
| `completed` | 完了かどうか | `BOOLEAN` | 必須。初期値は未完了 |
| `created_at` | 登録日時 | `DATETIME` | 自動で入る |
| `updated_at` | 更新日時 | `DATETIME` | 自動で入る |

### 2-2. ジャンル
`category` に入れられる値は 5 つである。

- デザイン
- マーケティング
- プログラミング
- 資格
- 就職活動

## 3. 画面一覧
画面は 6 つある。
URL の先頭は `http://localhost:8080` である。

| 画面名 | URL | 役割 |
|---|---|---|
| 一覧 | `/todos` | ToDo を一覧で見る |
| 登録 | `/todos/new` | 新しい ToDo を入力する |
| 登録の確認 | `/todos/confirm` | 入力内容を見て、登録するか決める |
| 編集 | `/todos/{id}/edit` | 1件を修正する |
| 編集の確認 | `/todos/{id}/confirm` | 修正内容を見て、更新するか決める |
| 削除の確認 | `/todos/{id}/delete` | 1件を見て、削除するか決める |
| トップページ | `/` | カード7で作ったものをそのまま使う |

`{id}` は、その 1 件の番号を入れる場所である。

## 4. 画面ごとの要件

### 4-1. 一覧画面 `/todos`
一覧画面では、登録した ToDo を表で表示する。
一覧画面では、やること、ジャンル、優先度、期限、完了かどうか、編集、削除を表示する。
一覧画面では、やることの名前で絞り込める。
一覧画面では、ジャンルで絞り込める。
一覧画面では、「完了あり」のボタンで完了した ToDo だけを表示できる。
一覧画面では、「期限が近い」順で並び替えできる。
一覧画面では、新規登録へ進める。

表に出す値は次のとおりである。

| 保存値 | 画面の表示 |
|---|---|
| `priority` の `1` | 高 |
| `priority` の `2` | 中 |
| `priority` の `3` | 低 |
| `completed` の `1` | 完了 |
| `completed` の `0` | 未完了 |

### 4-2. 登録画面 `/todos/new`
登録画面では、やることを入力できる。
登録画面では、メモを入力できる。
登録画面では、ジャンルを選べる。
登録画面では、優先度を選べる。
登録画面では、期限を入力できる。

| 項目 | 必須か | 補足 |
|---|---|---|
| やること | 必須 | 1行で入力する |
| メモ | 任意 | 1行で入力する |
| ジャンル | 必須 | 5つから選ぶ |
| 優先度 | 必須 | 高・中・低から選ぶ |
| 期限 | 任意 | 日付で入力する |

### 4-3. 登録の確認画面 `/todos/confirm`
登録の確認画面では、入力した 5 項目を確認できる。
登録の確認画面では、登録するか戻るかを選べる。
未入力の項目は、数字の `0` や空欄ではなく、「未入力」として扱う。

### 4-4. 編集画面 `/todos/{id}/edit`
編集画面では、登録画面と同じ 5 項目を編集できる。
編集画面では、開いたときに今登録されている内容をそのまま表示する。

### 4-5. 編集の確認画面 `/todos/{id}/confirm`
編集の確認画面では、編集した内容を確認できる。
編集の確認画面では、更新するか戻るかを選べる。

### 4-6. 削除の確認画面 `/todos/{id}/delete`
削除の確認画面では、削除する 1 件のやること、ジャンル、期限を表示する。
削除の確認画面では、削除するかやめるかを選べる。

## 5. 入力ルール
入力ルールは次のとおりである。

| 項目 | ルール | エラー表示 |
|---|---|---|
| やること | 空欄にしない | 「やることを入力してください」 |
| やること | 255文字以内 | 「やることは255文字以内で入力してください」 |
| メモ | 255文字以内 | 「メモは255文字以内で入力してください」 |
| ジャンル | 5つから選ぶ | 「ジャンルを選んでください」 |
| 優先度 | 選択肢から選ぶ | 「優先度を選んでください」 |
| 期限 | 日付として入力する | 日付の形式にしてください |

期限については、過去の日付も入力できる。

## 6. 操作後のメッセージ
登録のあと、画面の上に「登録しました」と表示する。
編集を保存したあと、画面の上に「保存しました」と表示する。
削除したあと、画面の上に「削除しました」と表示する。

## 7. 画面デザインの考え方
画面の見た目は CSS で整える。
ここでの CSS は「見た目を調整するための設定」である。

### 7-1. 使う CSS ファイル
- `src/main/resources/static/css/style.css`

### 7-2. 主に使う class 名
| class 名 | 使う場所 |
|---|---|
| `container` | 画面全体 |
| `page-title` | 画面タイトル |
| `todo-table` | 一覧表 |
| `flash` | 操作後のメッセージ |
| `search-bar` | 絞り込みの部分 |
| `form-row` | 入力欄の 1 行 |
| `form-label` | ラベル（項目名） |
| `form-input` | 入力欄 |
| `error` | エラーメッセージ |
| `confirm-value` | 確認画面の値 |
| `confirm-actions` | 確認画面のボタン部分 |
| `btn` | ボタン |
| `btn sub` | 補助ボタン |
| `link` | リンク |

## 8. この要件定義書の補足
この要件定義書は、`handout_app_spec.md` をもとに作成している。
仕様書にあった内容を、要件定義書として読みやすい形に並べ替えている。

補った点は次の 2 つである。

- 専門用語に短い言い換えを付けたこと
- 「未入力」や「見た目を調整するための設定」など、初心者向けの補足を付けたこと

