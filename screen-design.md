# 画面設計書

この画面設計書は、`handout_app_spec.md`、`requirements.md`、`db-design.md` をもとにした、ToDoアプリの画面まとめです。  
専門用語は、その場で短く言い換えを入れています。

## 1. 画面一覧

画面は7枚です。増やしません。

1. 一覧画面
2. 登録画面
3. 登録の確認画面
4. 編集画面
5. 編集の確認画面
6. 削除確認画面
7. トップページ

## 2. 画面共通ルール

- 画面上部にアプリ名または見出しを出します。
- 一覧画面では、ToDoの一覧を表で表示します。表は「行と列の形で並べる表示」です。
- 登録、編集、削除の前には確認画面をはさみます。
- エラーメッセージは、入力欄の近くに出します。
- 完了メッセージは、処理のあと一覧画面で出します。

## 3. 一覧画面

- 画面名: 一覧画面
- URL: `/todos`
- 見出し: `ToDo一覧`

### 画面に出す言葉

- ボタン
  - `新規登録`
  - `絞り込む`
  - `完了あり`
  - `編集`
  - `削除`
- 絞り込み欄のラベル
  - `やることの名前`
  - `ジャンル`
- 並び替えのリンク
  - `期限が近い順`
  - `期限が遠い順`

### 表に出す項目

- `やること`
- `ジャンル`
- `優先度`
- `期限`
- `状態`
- `編集`
- `削除`

### 表示ルール

- `優先度` は `1 / 2 / 3` を、それぞれ `高 / 中 / 低` として見せます。
- `状態` は `完了 / 未完了` として見せます。

### 画面遷移

- `新規登録` を押すと、登録画面へ移ります。
- `絞り込む` を押すと、一覧画面のまま絞り込み結果だけを更新します。  
  絞り込みとは「条件でしぼりこむこと」です。
- `完了あり` を押すと、完了したToDoだけを表示します。
- `編集` を押すと、選んだ1件の編集画面へ移ります。
- `削除` を押すと、選んだ1件の削除確認画面へ移ります。

### メッセージ

- 登録後: `登録しました`
- 編集後: `保存しました`
- 削除後: `削除しました`

- 画面名: トップページ
- URL: `/`
- 見出し: カード7で作ったものをそのまま使う

### 画面に出す言葉

- カード7で作ったものをそのまま使う

### 画面遷移

- カード7で作ったものをそのまま使う

### メッセージ

- カード7で作ったものをそのまま使う

## 4. 登録画面

- 画面名: 登録画面
- URL: `/todos/new`
- 見出し: `ToDo登録`

### 画面に出す言葉

- 入力項目ラベル
  - `やること`
  - `メモ`
  - `ジャンル`
  - `優先度`
  - `期限`
- ボタン
  - `確認する`
  - `一覧へ`

### 入力内容

- `やること` は必須です。必須は「必ず入れること」です。
- `メモ` は任意です。任意は「入れなくてもよいこと」です。
- `ジャンル` は必須です。
- `優先度` は必須です。
- `期限` は任意です。

### エラー文

- `やることを入力してください`
- `やることは255文字以内で入力してください`
- `メモは255文字以内で入力してください`
- `ジャンルを選んでください`
- `優先度を選んでください`
- `日付の形式にしてください`

### 画面遷移

- `確認する` を押すと、登録の確認画面へ移ります。
- `一覧へ` を押すと、一覧画面へ戻ります。

## 5. 登録の確認画面

- 画面名: 登録の確認画面
- URL: `/todos/confirm`
- 見出し: `ToDo登録内容の確認`

### 画面に出す言葉

- ボタン
  - `登録する`
  - `書き直す`
- 確認項目
  - `やること`
  - `メモ`
  - `ジャンル`
  - `優先度`
  - `期限`

### 画面遷移

- `登録する` を押すと、登録処理を行って一覧画面へ移ります。
- `書き直す` を押すと、登録画面へ戻ります。入力した内容はそのまま残します。

### メッセージ

- 登録後: `登録しました`

## 6. 編集画面

- 画面名: 編集画面
- URL: `/todos/{id}/edit`
- 見出し: `ToDo編集`

### 画面に出す言葉

- 入力項目ラベル
  - `やること`
  - `メモ`
  - `ジャンル`
  - `優先度`
  - `期限`
  - `完了`
- ボタン
  - `確認する`
  - `一覧へ`

### 入力内容

- 登録画面と同じです。
- 画面を開いたときは、もとの値を初期表示します。初期表示は「最初から入れて見せること」です。

### エラー文

- 登録画面と同じです。

### 画面遷移

- `確認する` を押すと、編集の確認画面へ移ります。
- `一覧へ` を押すと、一覧画面へ戻ります。

## 7. 編集の確認画面

- 画面名: 編集の確認画面
- URL: `/todos/{id}/confirm`
- 見出し: `ToDo編集内容の確認`

### 画面に出す言葉

- ボタン
  - `保存する`
  - `書き直す`
- 確認項目
  - `やること`
  - `メモ`
  - `ジャンル`
  - `優先度`
  - `期限`
  - `完了`

### 画面遷移

- `保存する` を押すと、更新処理を行って一覧画面へ移ります。
- `書き直す` を押すと、編集画面へ戻ります。入力した内容はそのまま残します。

### メッセージ

- 更新後: `保存しました`

## 8. 削除確認画面

- 画面名: 削除確認画面
- URL: `/todos/{id}/delete`
- 見出し: `ToDo削除確認`

### 画面に出す言葉

- ボタン
  - `削除`
  - `やめる`
- 確認項目
  - `やること`
  - `ジャンル`
  - `期限`

### 画面遷移

- `削除` を押すと、削除処理を行って一覧画面へ移ります。
- `やめる` を押すと、一覧画面へ戻ります。

### メッセージ

- 削除後: `削除しました`

## 9. 入力項目とDBの対応

`DB` は「データをしまう場所」です。  
画面の入力項目と、`todos` テーブルの列は次の対応です。

- `やること` → `title`
- `メモ` → `detail`
- `ジャンル` → `category`
- `優先度` → `priority`
- `期限` → `due_date`
- `完了` → `completed`

自動で入るものは画面に出しません。

- `id`
- `created_at`
- `updated_at`

## 10. 画面遷移の全体像

```text
一覧画面
  ├─ 新規登録 → 登録画面 → 確認する → 登録の確認画面 → 登録する → 一覧画面
  ├─ 編集 → 編集画面 → 確認する → 編集の確認画面 → 保存する → 一覧画面
  └─ 削除 → 削除確認画面 → 削除 → 一覧画面
```


